export interface DashboardCardProps {
  icon: string;
  iconColor: string;
  iconBgColor: string;
  title: string;
  subtitle: string;
  subtitleHighlight?: string;
  onPress?: () => void;
}
