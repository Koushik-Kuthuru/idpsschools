import StudentPortalLayout from "@/components/portals/StudentPortalLayout";

export default function StudentLayout({ children }: { children: React.ReactNode }) {
  return <StudentPortalLayout>{children}</StudentPortalLayout>;
}
