export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  gutter: 16,
  maxWidth: 390,
  headerHeight: 56,
  bottomNavHeight: 56,
  /** Fixed offset for FABs inside tab screens (above bottom nav). */
  fabBottom: 24,
} as const;
